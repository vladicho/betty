{
  "name": "betty",
  "version": "0.1.0",
  "private": false,
  "description": "Simulador e futura plataforma de controle para maquina automatica de corte de tecido.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test": "node --test",
    "check": "node --check web/app.js && node --check src/core/geometry.js && node --check src/core/path-planner.js && node --check src/core/machine.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
